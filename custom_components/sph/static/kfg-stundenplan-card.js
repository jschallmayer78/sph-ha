class KfgStundenplanCard extends HTMLElement {
  setConfig(config){this.config=config||{};this.attachShadow({mode:"open"});}
  set hass(hass){this._hass=hass;if(!this.config||!this.shadowRoot)return;const entity=this._findEntity(hass),attrs=entity?.attributes||{},days=Array.isArray(attrs.eigener_plan)?attrs.eigener_plan:[],week=attrs.wochenkennung,title=this.config.title||(attrs.kind_kürzel?`Stundenplan – ${attrs.kind_kürzel}`:"Stundenplan");this.shadowRoot.innerHTML=`<style>:host{display:block}.content{padding:12px 16px}section{margin-bottom:16px}h3{margin:0 0 8px}.lesson{display:flex;gap:12px;padding:8px 0;border-bottom:1px solid var(--divider-color)}.lesson-options{display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:8px;flex:1}.time{min-width:92px;color:var(--secondary-text-color);white-space:nowrap}.main{display:flex;flex-direction:column;gap:2px}.main small{color:var(--secondary-text-color)}.badges{display:flex;gap:4px;flex-wrap:wrap;margin-top:3px}.badge{display:inline-flex;align-items:center;padding:1px 6px;border-radius:8px;background:var(--primary-color);color:var(--text-primary-color);font-size:.78rem;font-weight:600}.empty{color:var(--secondary-text-color)}.error{padding:16px;color:var(--error-color)}.kfg{font-size:.72rem;color:var(--secondary-text-color);margin-left:6px}</style><ha-card header="${this._escape(title)}"><div class="content"><div class="kfg">KFG Anpassung${week?` · Woche ${this._escape(week)}`:""}</div>${entity?days.slice(0,5).map((day,i)=>`<section><h3>${["Montag","Dienstag","Mittwoch","Donnerstag","Freitag"][i]}</h3>${this._renderDay(day,week)}</section>`).join(""):`<div class="error">Kein passender Stundenplan gefunden.</div>`}</div></ha-card>`;}
  _isActive(lesson,week){const badge=lesson?.badge;if(badge===null||badge===undefined||badge===""||!week)return true;const values=Array.isArray(badge)?badge:[...String(badge).split(/[,;/|]+/).map(x=>x.trim()).filter(Boolean)];return values.some(x=>x.toUpperCase()===String(week).toUpperCase());}
  _renderDay(day,week){const lessons=(day||[]).filter(x=>this._isActive(x,week));if(!lessons.length)return`<span class="empty">Kein Unterricht</span>`;const groups=[],byTime=new Map();for(const lesson of lessons){const key=`${lesson.start||""}|${lesson.end||""}`;if(!byTime.has(key)){const group={start:lesson.start||"",end:lesson.end||"",lessons:[]};byTime.set(key,group);groups.push(group);}byTime.get(key).lessons.push(lesson);}return groups.map(group=>`<div class="lesson"><span class="time">${this._escape(group.start)}–${this._escape(group.end)}</span><div class="lesson-options">${group.lessons.map(x=>`<div><span class="main"><b>${this._escape(x.fach||x.subject||"Unterricht")}</b>${this._renderBadges(x.badge)}<small>${this._escape(x.teacher||"")}${x.room?" · "+this._escape(x.room):""}</small></span></div>`).join("")}</div></div>`).join("");}
  _renderBadges(value){if(value===null||value===undefined||value==="")return"";const values=Array.isArray(value)?value:String(value).split(/[,;/|]+/).map(x=>x.trim()).filter(Boolean);return values.length?`<span class="badges">${values.map(x=>`<span class="badge">(${this._escape(x)})</span>`).join("")}</span>`:"";}
  _findEntity(hass){const configured=this.config.sensor||this.config.entity;if(configured&&hass.states[configured])return hass.states[configured];if(!this.config.child)return hass.states["sensor.stundenplan"];const wanted=String(this.config.child).toLowerCase();return Object.values(hass.states).find(s=>s.entity_id.startsWith("sensor.")&&s.attributes?.kind_kürzel?.toString().toLowerCase()===wanted);}
  _escape(value){const d=document.createElement("div");d.textContent=String(value??"");return d.innerHTML;}getCardSize(){return 6;}}
customElements.define("kfg-stundenplan-card",KfgStundenplanCard);window.customCards=window.customCards||[];window.customCards.push({type:"kfg-stundenplan-card",name:"KFG Stundenplan (KFG Anpassung)",description:"Persönlicher Stundenplan mit KFG-Wochenfilterung"});
